---
- name: Set logging
  become: true
  community.general.ufw:
    logging: 'off'
- name: Limit ssh connections
  become: true
  community.general.ufw:
    rule: limit
    port: ssh
    proto: tcp
- name: Enable firewall
  become: true
  community.general.ufw:
    state: enabled
